# Copie para a RAIZ do projeto no cluster (nao para dentro de slurm/):
#     cd /raid/user_juliadollis/projects/depth-riemannian
#     cp slurm/.env.example .env && ${EDITOR:-nano} .env && chmod 600 .env
#
# O run_riemann_setup.slurm procura o .env nesta ordem:
#     $ENV_FILE > <raiz do projeto>/.env > $SLURM_SUBMIT_DIR/.env > $PWD/.env

# Token do Hugging Face, usado apenas para baixar os pesos depth_pro.pt.
# Gere em: https://huggingface.co/settings/tokens
HF_TOKEN=
