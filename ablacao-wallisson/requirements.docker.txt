# Dependências para a imagem de container.
# IMPORTANTE: NÃO incluir torch/torchvision aqui — a imagem base
# (pytorch/pytorch:2.4.1-cuda12.4-cudnn9) já traz o PyTorch com CUDA compilado.
# Reinstalá-lo via pip pode sobrescrever com uma build CPU e quebrar a GPU.

numpy>=1.26
opencv-python-headless>=4.9
h5py>=3.10
Pillow>=10.0
optuna>=3.6
tqdm>=4.66
pandas>=2.2
scipy>=1.12
matplotlib>=3.8
